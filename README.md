# HR Assistant

Система автоматизации HR-процессов с использованием ИИ для оценки резюме и генерации описаний вакансий.

## 🌟 Основные возможности

- Автоматическая оценка соответствия резюме и вакансий
- Генерация профессиональных описаний вакансий
- Веб-интерфейс для удобной работы с системой

## 🏗 Архитектура проекта

Проект состоит из следующих компонентов:

### Core API
Центральный сервис, обрабатывающий входящие запросы и координирующий работу других сервисов.
- FastAPI backend
- Асинхронная обработка через RabbitMQ


### Resume Evaluation Service
Сервис для анализа и оценки резюме.
- Сравнение резюме с требованиями вакансии
- Выставление оценки соответствия


### Job Description Service
Сервис для работы с описаниями вакансий.
- Генерация структурированных описаний вакансий
- Анализ требований и обязанностей
- Форматирование текста вакансии


### Frontend
Веб-интерфейс для взаимодействия с системой.
- Пользовательский интерфейс
- Интерактивные формы для работы с резюме и вакансиями
- Визуализация результатов анализа


## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Python 3.12.5 (рекомендуемый)

### Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd hr-assistant
```

2. Сконфигурируйте окружение:

Система настраивается через переменные окружения (.env):

Для примера воспользуйтесь файлом `.env.example` и переименуйте его в `.env`.

###### LLM Конфигурация
```env
OPENAI_MODEL_NAME=your-model-name # Название модели
OPENAI_API_KEY=your-api-key # API ключ
OPENAI_BASE_URL=<your-base-url> # Базовый URL для API модели
```

###### RabbitMQ Конфигурация
```env
RABBITMQ_DEFAULT_USER=guest # Имя пользователя
RABBITMQ_DEFAULT_PASS=guest # Пароль пользователя
RABBITMQ_DEFAULT_VHOST=/ # Виртуальный хост
RABBITMQ_PORT=5672 # Порт для подключения к RabbitMQ
RABBITMQ_UI_PORT=15672 # Порт для доступа к интерфейсу RabbitMQ
```

###### UI Настройки
```env
STREAMLIT_SERVER_ADDRESS=0.0.0.0 # IP-адрес для запуска Streamlit
STREAMLIT_SERVER_PORT=8501 # Порт для запуска Streamlit
STREAMLIT_SERVER_HEADLESS=true # Запуск Streamlit в режиме без графического интерфейса
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false # Отключение сбора статистики использования браузера
STREAMLIT_THEME_BASE="light" # Базовая тема для Streamlit - всегда светлая
```

##### Core API Настройки
```env
CORE_API_PORT=8000 # Порт для запуска Core API
```

3. Запустите приложение:

```
docker compose up --build # В корне проекта
```
