{{- if or .Values.rbac.enabled (and .Values.global.rbac .Values.global.rbac.enabled) }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "postgres.fullname" . }}-secret-reader
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["{{ include "postgres.secretName" . }}"]
    verbs: ["get", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "postgres.fullname" . }}-secret-binding
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "postgres.serviceAccountName" . }}
roleRef:
  kind: Role
  name: {{ include "postgres.fullname" . }}-secret-reader
  apiGroup: rbac.authorization.k8s.io
{{- end }}





