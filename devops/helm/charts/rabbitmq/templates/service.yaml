---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-rabbitmq
spec:
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
    - name: ui
      port: {{ .Values.uiPort }}
      targetPort: {{ .Values.uiPort }}


