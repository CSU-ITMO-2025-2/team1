---
{{- include "job-description-service.validateValues" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "job-description-service.deploymentName" . }}
  labels:
    {{- include "job-description-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "job-description-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "job-description-service.selectorLabels" . | nindent 8 }}
        egress: openai-via-squid
    spec:
      {{- if or .Values.rbac.enabled (and .Values.global.rbac .Values.global.rbac.enabled) }}
      serviceAccountName: {{ include "job-description-service.serviceAccountName" . }}
      {{- end }}
      containers:
        - name: job-descr-worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "job-description-service.configMapName" . }}
            - secretRef:
                name: {{ include "job-description-service.secretName" . }}
          {{- if .Values.probes }}
          livenessProbe:
            {{- toYaml .Values.probes.liveness | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.probes.readiness | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

